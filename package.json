{
  "name": "wisp",
  "version": "1.0.0",
  "private": true,
  "description": "Ephemeral, mood-colored journaling",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel deploy --prod"
  }
}
